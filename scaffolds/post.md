---
title: {{ title }}
date: {{ date }}
tags:
---

站点总书名、作者、导航等在 `_config.yml` / `_config.novel.yml`；**本章标题**只改上面 `title:`。**正文**从下面开始写。章首题记建议用 Markdown 引用块 `>`。`_posts` 下正文章文件推荐命名为 `NN-dahuangya.md`（两位章序，勿加日期前缀）。
